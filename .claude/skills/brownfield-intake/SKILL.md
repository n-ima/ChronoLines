---
name: brownfield-intake
description: 既存のコードベース(brownfield)にこのハーネスを導入する手順。tools/intake-app.py で既存アプリをテンプレート複製の app/ 配下へ機械的に取り込み(経路A)、またはgit履歴を保全したい既存リポジトリへハーネス一式を注入し(経路B)、実装済みコードからas-is要件・as-isアーキテクチャをdocsに逆起こしして、コードとの整合を検証してから差分駆動の改修サイクルに接続する。「既存アプリにハーネスを入れたい」「途中からハーネスを使いたい」「多数の既存アプリに展開したい」ときに使う。
---

このスキルの本文の正は `.github/skills/brownfield-intake/SKILL.md` です。
それを読み、その手順・チェックリストに従ってください(このファイルはClaude Codeが
`.claude/skills/` しか探索しないために置いてある薄いポインタです)。
